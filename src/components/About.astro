---
import { personalInfo, aboutStats } from "../data/index";
import { ExternalLink, Code2, MapPin, Mail } from "@lucide/astro";
---

<section id="about" class="relative">
  <div class="section-container">
    <!-- Section header -->
    <div class="reveal mb-16">
      <p class="text-sm font-mono mb-2" style="color: var(--color-accent)">
        01. sobre mí
      </p>
      <h2 class="section-title">Un poco sobre mí</h2>
    </div>

    <div class="grid md:grid-cols-2 gap-16 items-center">
      <!-- Left: avatar + stats -->
      <div class="reveal-left">
        <!-- Avatar container -->
        <div class="relative w-fit mx-auto md:mx-0">
          <div
            class="w-64 h-64 rounded-3xl overflow-hidden relative"
            style="border: 2px solid var(--color-border)"
          >
            <!-- Placeholder avatar — reemplaza con tu foto -->
            <div
              class="w-full h-full flex items-center justify-center"
              style="background: linear-gradient(135deg, rgba(124,58,237,0.15) 0%, rgba(236,72,153,0.1) 100%)"
            >
              <Code2 class="w-20 h-20" style="color: var(--color-accent)" />
            </div>
          </div>
          <!-- Decorative corner -->
          <div
            class="absolute -bottom-4 -right-4 w-20 h-20 rounded-2xl -z-10"
            style="background-color: var(--color-accent); opacity: 0.15"
          >
          </div>
          <div
            class="absolute -top-4 -left-4 w-12 h-12 rounded-xl -z-10"
            style="background-color: var(--color-accent); opacity: 0.1"
          >
          </div>
        </div>

        <!-- Stats -->
        <div class="grid grid-cols-2 gap-4 mt-8">
          {
            aboutStats.map((stat) => (
              <div
                class="rounded-2xl p-4 text-center"
                style="background-color: var(--color-surface); border: 1px solid var(--color-border)"
              >
                <p
                  class="text-2xl font-bold mb-1 gradient-text"
                >
                  {stat.value}
                </p>
                <p class="text-xs" style="color: var(--color-text-muted)">
                  {stat.label}
                </p>
              </div>
            ))
          }
        </div>
      </div>

      <!-- Right: content -->
      <div class="reveal space-y-6">
        <p class="text-lg leading-relaxed" style="color: var(--color-text-muted)">
          {personalInfo.bio}
        </p>

        <p class="text-base leading-relaxed" style="color: var(--color-text-muted)">
          Cuando no estoy programando, me encuentras explorando nuevas tecnologías,
          contribuyendo a proyectos open source, o aprendiendo sobre arquitectura
          de software y diseño de sistemas.
        </p>

        <p class="text-base leading-relaxed" style="color: var(--color-text-muted)">
          Me apasiona escribir código que no solo funciona, sino que es legible,
          mantenible y escalable. Creo firmemente que la mejor documentación es
          el código bien escrito.
        </p>

        <!-- Info grid -->
        <div
          class="grid grid-cols-1 gap-3 pt-4 border-t"
          style="border-color: var(--color-border)"
        >
          <div class="flex items-center gap-3">
            <MapPin class="w-5 h-5 shrink-0" style="color: var(--color-accent)" />
            <div>
              <p class="text-xs" style="color: var(--color-text-muted)">Ubicación</p>
              <p class="text-sm font-medium">{personalInfo.location}</p>
            </div>
          </div>
          <div class="flex items-center gap-3">
            <Mail class="w-5 h-5 shrink-0" style="color: var(--color-accent)" />
            <div>
              <p class="text-xs" style="color: var(--color-text-muted)">Email</p>
              <a
                href={`mailto:${personalInfo.email}`}
                class="text-sm font-medium transition-colors duration-200"
                style="color: var(--color-accent)"
              >
                {personalInfo.email}
              </a>
            </div>
          </div>
        </div>

        <a href="/cv" target="_blank" rel="noopener noreferrer" class="btn-primary w-fit">
          Ver CV
          <ExternalLink class="w-4 h-4" />
        </a>
      </div>
    </div>
  </div>
</section>
